Algoritmo "soma"

Var
        a, b, soma:inteiro

Inicio

        Escreval("Digite o valor de a: ")
        Leia(a)
        Escreval("Digite o valor de b: ")
        Leia(b)

        soma <- a + b
        
        Escreval("SOMA: ",soma)
        

Fimalgoritmo